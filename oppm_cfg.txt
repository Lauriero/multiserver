["Lauriero/multiserver"] = {
      ["multiserver"] = {
        files = {
          ["master/multiserver.lua"] = "/",
          ["master/slave_server_script.lua"] = "/"
        },
        name = "multiserver",--This is for "oppm info"
        description = "Package for make calculation faster with many computers",
        authors = "Lauriero"
        hidden = true, -- Add this optional line to make your package not visible in "oppm list", useful for custom dependency libraries
    },
